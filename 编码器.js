const x1 = '富强,民主,文明,和谐,自由,平等,公正,法治,爱国,敬业'
const x2 = '诚信,友善';      常量 x1l = 有序列表(字符.分割(x1,","))  常量 x2l = 有序列表(字符.分割(x2,","));    函数 取分隔() {    return x2l.get(数学.随机数(0,1));    }    函数 取所有(i) {  
let r = x1l.get(i);    返 r    }     函数 解码(str) {        let o = StringBuilder();        let oo = "";         for (let i =0;i<str.length();) {                  let bs = str.substring(i,i+2);                  if (x1l.检查(bs)) {                  oo += x1l.indexOf(bs);               } else if(x2l.检查(bs)) {                  if (oo == "") continue;                  o.append(Character(oo));                  oo = "";                  }                           i += 2;     }          返 o.toString(); 
} 
函数 编码(str) {        let o = StringBuilder();         for (let i =0;i<str.length();i++) {                  let bs = str.charAt(i).toString();                  let bb = 字符.转换(bs);              for (let bi = 0;bi < bb.length();bi++) {                          o.append(取所有(bb.substring(bi,bi + 1)));                      }                  o.append(取分隔());                    }          返 o.toString(); 
}              常量 $布局 = 打开布局(新 布局_滚动界面(当前界面));          置标题("某编码器");          取导航按钮().结束界面(当前界面);          $布局.置填充("填充");          常量 $输入 = 新 输入框($布局);          $输入.置默认文本("输入文本 ~");          常量 $编码 = 新 按钮($布局);          $编码.置文本("编码");          $编码.置宽度("最大");          $编码.置单击事件(函数() {                  try {                  $输入.置文本(编码($输入.取文本()));                  提示.普通("编码成功 ~");                  } catch(E) {                  提示.普通("编码错误 ~");                  }      });          常量 $解码 = 新 按钮($布局);          $解码.置宽度("最大");          $解码.置文本("解码");          $解码.置单击事件(函数() {                  try {               $输入.置文本(解码($输入.取文本()));                  提示.普通("解码成功 ~");                  } catch(E) {                  提示.警告("解码失败 ~");                  }      });
